import { css } from '@emotion/core'
import { ThemedCssCreator } from '../../../theming'

export const wrapperCss = css`
  width: fit-content;
  font-size: 22px;
  font-style: italic;
`
export const quoteCss: ThemedCssCreator = css`
  width: 100%;
  margin-bottom: 10px;
`

export const authorCss = css`
  margin-right: 45px;
  text-align: right;
`
